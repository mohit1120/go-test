pipeline {
  agent {
    docker { image 'golang:latest' }
  }
  environment {
    GOCACHE = '/cache'
  }
  stages {
    stage('Test') {
      steps {
        sh 'go version'
        sh 'go test ./check'
      }
    }
  }
}