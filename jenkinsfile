pipeline {
  agent {
    docker { image 'golang:latest' }
  }
  environment {
    GOCACHE = "${WORKSPACE}/go-cache" // Set the cache directory within the Jenkins workspace
  }
  stages {
    stage('Test') {
      steps {
        sh 'go version'
        sh 'go test ./check'
      }
    }
  }
}