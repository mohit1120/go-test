pipeline {
  agent {
    docker {
      image 'golang:latest'
      args '-v /host/cache:/root/.cache'
    }
  }
  stages {
    stage('Test') {
      steps {
        sh 'go version'
        sh 'go test ./check'
      }
    }
  }
}
